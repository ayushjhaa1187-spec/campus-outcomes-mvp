/**
 * Configuration for the API URL.
 *
 * To use a custom API URL (e.g., for split deployment or local testing):
 * 1. Copy this file to 'config.js' in the same directory.
 * 2. Update the API_BASE_URL value.
 * 3. Include 'config.js' in your HTML file before 'api.js'.
 *    <script src="js/config.js"></script>
 *    <script src="js/api.js"></script>
 */

window.APP_CONFIG = {
  // Example: 'https://your-backend-app.onrender.com/api'
  API_BASE_URL: null
};
